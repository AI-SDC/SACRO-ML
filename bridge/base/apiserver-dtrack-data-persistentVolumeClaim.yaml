#! apiserver-dtrack-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: apiserver-dtrack-data
    namespace: sacro-ml
    labels:
        com.docker.compose.service: apiserver
        com.docker.compose.volume: dtrack-data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi

# Scenario to be run
user_story: 1

### Details of experiments and files - please replace these with the relevant filenames

# Path to the dataset used to train the model
dataset_filename: "./user_stories_resources/dataset_26_nursery.csv"

# Name of the file (or directory for keras) containing the saved model
target_model: "model.pkl"

# Location of the directory with all the files needed to assess this release
outfile: "summary.txt"
attack_results: "attack_results.json"
training_artefacts_dir: "training_artefacts"

# User story 1, 2 or 3: name of the target results file generated by aisdc
target_results: "target.json"

# User story 3
x_train_path: "trainX.txt"
y_train_path: "trainy.txt"
x_test_path: "testX.txt"
y_test_path: "testy.txt"
attack_output_name: "attack_output"

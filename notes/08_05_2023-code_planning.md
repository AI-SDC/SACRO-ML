# Meeting to discuss ongoing plans / technical implementation issues
SM, JS, MA, RP, SR,

Code finishing from previous sprint:
- 4 PRs left over from SM, SR/RP to work through them asap

ST hs created milestone for release 1.0.5
- need to document json format: top level is details of target algorithm etc, then "attack logger "dictionary"
- bring AIA and also the safemodel files into that fold (sM/RP)
- improve the tests so they test functionality not just coveragge (YJ for some, MA for safemodels)
- SM has writtn PR that provides optino to specify attack hyperparameters via config file or command line.
  would be good to standardise this for AIA as well
  - config file makes sense for lots of reasons. What is it going to be called?
  - if we are going to have a 'higher level' config file where you specify what attacks you run, is that the same for reseachers as for the TRE output checkers.
  -
Plans for next sprint
- need user stories JS/MA to co-ordinate
-
